@model Sub.Models.BranchViewModel
@{
    ViewBag.Title = "Detail";
}
<!-- Section: Blog -->
<section>
    <div class="container">
        <div class="row">
            <div class="col-md-9">
                <div class="blog-posts">
                    <div class="col-md-12">
                        @if (System.Globalization.CultureInfo.CurrentCulture.Name == "en-US")
                        {
                            @Html.Raw(Model.news.BodyMon)
                        }
                        else
                        {
                            @Html.Raw(Model.news.BodyMon)
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
